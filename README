# puppet-gnutls

simple key generation utility

# example 
    gnutls::generate_key{'generate app keys':
      certfile    => '/opt/app/certs/cert.out',
      keyfile     => '/opt/app/certs/key.out',
      user        => 'root',
    }

# license

MIT

# warranty

none, express or implied
